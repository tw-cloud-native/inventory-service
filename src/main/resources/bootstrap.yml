server:
  port: 9081

spring:
  application:
    name: inventory-service
  cloud:
    config:
      fail-fast: true
      enabled: false
    bus:
      enabled: true
      trace:
       enabled: true
